# Moved

Git branch, worktree, and QC/QA checkout rules now live in the **`mstar-branch-worktree`** skill (**`SKILL.md`** only; rules inlined).

Do not duplicate rules here; update `mstar-branch-worktree` only.
