# Agent 知识库索引（重定向）

**索引、文档映射、归属、更新规则与设计审查清单**的正文均在 **`~/.config/opencode/AGENTS.md`**（全局每会话加载）。

专题深度文档仍在 `~/.config/opencode/docs/agents/*.md`（如 `harness-loop.md`、`review-harness.md` 等）。

本路径仅保留给历史链接与书签兼容。
